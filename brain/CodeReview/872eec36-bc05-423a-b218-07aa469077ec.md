---
id: 872eec36-bc05-423a-b218-07aa469077ec
name: Implement New Dashboard Layout with Horizontal Agents, Activity Feed & DnD Task Board
label: CodeReview
source: agent-os-brain
---
# Implement New Dashboard Layout with Horizontal Agents, Activity Feed & DnD Task Board

## Description

Redesign the dashboard layout based on the following spec:

1. **Top Section - Agent Bar**: Display all agents listed horizontally across the top of the dashboard in a single row (use flexbox/horizontal scroll if needed).

2. **Bottom Section - 50/50 Split**:
   - **Left Side (50%) - Activity Feed**: Render the existing Activity Feed component in the left half.
   - **Right Side (50%) - Task Board**: Build a Kanban-style drag-and-drop board with 4 columns:
     - Inbox
     - In Progress
     - Review
     - Done
   Each column should display small cards representing tasks/items. Cards should show relevant info (title, agent, status). Use the same DnD library already in the project (or install dnd-kit if none exists). Columns and cards should be styled consistently with the existing Board component if one exists.

3. **Styling**: Ensure the layout is responsive and visually clean. Use the existing design system/theme tokens.

Reference the audit findings from the research subtask for file paths and existing components to reuse.

## Properties

| Field | Value |
|---|---|
| output | Good, I can see the full structure. Now let me read all the critical files simultaneously. |
| tokens_used | 4669 |
| tools_used | Files written: [], Commands: [] |
| model_used | claude-sonnet-4-6 |
| task_type | review |

---
*Auto-generated from Neo4j brain graph. Edit at your own risk — overwritten on next sync.*